import React from 'react';
import { Text, View } from 'react-native';

const HolaMundo = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
      }}>
      <Text style={{fontSize: 50, textAlign: 'center'}}>Wena Mundo</Text>
    </View>
  );
};

export default HolaMundo;
